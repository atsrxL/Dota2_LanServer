-- Replaced transactionally by the LXC panel before every launch.
-- Tools preview is explicitly NOT a panel-attested game session.
return {
    schema = 1,
    session = "00000000000000000000000000000000",
    client_revision = "lanlab-130.1",
    source_sha256 = "tools-preview-not-deployed",
    bot_available = false,
    bot_globals = {}
}
